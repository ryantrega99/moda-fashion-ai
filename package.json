
{
  "name": "modafx-studio",
  "version": "3.2.0",
  "private": true,
  "description": "High-Fidelity AI Fashion Visual Engine",
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p .public && cp index.html index.tsx types.ts App.tsx vercel.json metadata.json .public/ 2>/dev/null || true"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "@google/genai": "^1.35.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
